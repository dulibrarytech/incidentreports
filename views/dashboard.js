dashboard = {

	element: "#content",
	template: "dashboard",

	init: function(tableData) {
		
		// namestring

		this.construct(tableData);
	},

	construct: function(tableData) {

		//alert(tableData[0]);
	}
};

